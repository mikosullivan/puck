# SQL

~~~vibecode
{"vibecode": {
	"doc": "ideas_drinian_with_sqlite_sql",
	"role": "display page for the authoritative Drinian DDL. The SQL is pulled in from src/drinian.sql at render time via Orlando's file: directive — this page is always the latest committed schema. Edits happen in the .sql file, not here.",
	"status": "display-only — the source of truth is src/drinian.sql"
}}
~~~

The authoritative Drinian DDL. Edited in [src/drinian.sql](src/drinian.sql); shown here.

<!-- file: src/drinian.sql -->
