# Frames

~~~vibecode
{"vibecode": {
	"doc": "ideas_frames",
	"role": "index for the frames/ ideas directory — pages walking through how source lands in the CVM as an object graph + frame 0, using progressively richer examples.",
	"status": "index"
}}
~~~

Walkthroughs of what the CVM tables look like at each stage of loading a Caspian program, using progressively richer example scripts.

- [hello-world](./hello-world) — the smallest possible program (`puts 'hello world'`). The on-ramp.
- [if-statement](./if-statement) — adds `if / elsif / else` control flow to the same three-table walk.
