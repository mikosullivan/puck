# Caspian security model (moved)

The security model has moved to [requirements/security/model/](https://puck.uno/requirements/security/model/). This page remains as a pointer so old links resolve.

The worked example — the [tires manufacturing sim](tires) — is still here in `ideas/security/model/` as illustrative material that accompanies the spec.
