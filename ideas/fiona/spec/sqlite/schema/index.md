# Schema

~~~vibecode
{"vibecode": {
	"doc": "ideas_fiona_spec_sqlite_schema",
	"role": "Live-rendered view of the Fiona SQLite schema. Source of truth is ../../../build/sqlite/lua/src/fiona.sql; this page reads that file on every render so edits show up immediately.",
	"status": "live reference"
}}
~~~

Live-rendered view of the Fiona SQLite schema. Source of truth is `../../../build/sqlite/lua/src/fiona.sql`; this page reads it on every render, so edits to the SQL show up here immediately.

<!-- file: ../../../build/sqlite/lua/src/fiona.sql -->
