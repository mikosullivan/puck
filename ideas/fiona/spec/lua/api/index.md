# API layer

~~~vibecode
{"vibecode": {
	"doc": "ideas_fiona_spec_lua_api",
	"role": "Placeholder. The method-surface spec that used to live here has moved to the SQLite implementation page at ../../sqlite/, since the methods are properties of the SQLite backend, not a backend-agnostic API layer. This page stays as a landing spot if a genuine cross-backend API abstraction ever emerges.",
	"status": "empty — content moved to ../../sqlite/"
}}
~~~

Content moved to [../../sqlite/](../../sqlite/). The method surface belongs there — it's the SQLite backend's methods, not a backend-agnostic API layer.
