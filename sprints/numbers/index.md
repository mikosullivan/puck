~~~vibecode
{"doc": "sprint-index", "sprint": "numbers",
	"role": "Enforce 'all numbers are floats' end-to-end. The Number spec already says the language has no int/float distinction, but leaves an implementation-detail hedge that the engine 'may' store integer-shaped values more efficiently. This sprint closes that: internal representation is float too, no exception. Three known sites to sweep — schema check, Number spec hedge, and write-path coercion.",
	"status": "kicked off — scope pending"}
~~~

# numbers

Placeholder. Scope to be filled in.
