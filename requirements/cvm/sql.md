# SQL

~~~vibecode
{"vibecode": {
	"doc": "requirements_cvm_sql",
	"role": "display page for the authoritative CVM DDL. The SQL is pulled in from src/engine/cvm.sql at render time via Orlando's file: directive — this page is always the latest committed schema. Edits happen in the .sql file, not here.",
	"status": "display-only — the source of truth is src/engine/cvm.sql"
}}
~~~

The authoritative CVM DDL. Edited in [src/engine/cvm.sql](../../src/engine/cvm.sql); shown here.

<!-- file: ../../src/engine/cvm.sql -->
