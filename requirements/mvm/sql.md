# SQL

~~~vibecode
{"vibecode": {
	"doc": "requirements_mvm_sql",
	"role": "display page for the authoritative MVM DDL. The SQL is pulled in from src/engine/mvm.sql at render time via Orlando's file: directive — this page is always the latest committed schema. Edits happen in the .sql file, not here.",
	"status": "display-only — the source of truth is src/engine/mvm.sql"
}}
~~~

The authoritative MVM DDL. Edited in [src/engine/mvm.sql](../../src/engine/mvm.sql); shown here.

<!-- file: ../../src/engine/mvm.sql -->
