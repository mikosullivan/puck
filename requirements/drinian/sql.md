# SQL

~~~vibecode
{"vibecode": {
	"doc": "requirements_drinian_sql",
	"role": "display page for the authoritative Drinian DDL. The SQL is pulled in from src/engine/drinian.sql at render time via Orlando's file: directive — this page is always the latest committed schema. Edits happen in the .sql file, not here.",
	"status": "display-only — the source of truth is src/engine/drinian.sql"
}}
~~~

The authoritative Drinian DDL. Edited in [src/engine/drinian.sql](../../src/engine/drinian.sql); shown here.

<!-- file: ../../src/engine/drinian.sql -->
